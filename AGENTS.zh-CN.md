# 代理说明

> 非生效译文：本文件只是 `AGENTS.md` 的同目录中文翻译。代理应以原始
> `AGENTS.md` 为准，本译文不作为规则来源。

仅在 `.agents/` 中的规则与当前工作相关时读取对应文件。

- 创建或编辑 Git 提交信息前，读取 `.agents/commit-convention.md`。
- 添加或编辑源码前，读取 `.agents/comment-convention.md`。
- 运行 PlatformIO 命令、选择环境、诊断工具链或修改 PlatformIO 相关配置前，读取 `.agents/platformio-toolchain-discovery.md`。
